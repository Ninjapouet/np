


module UDP = struct

  type address = Unix.inet_addr
  type port = int

  (* let make :
   *   ?bufsize:int ->
   *   ?buffer:bytes ->
   *   address -> port ->
   *   (address * port * string) Lwt_stream.t ->
   *   (address * port * string) Lwt_stream.t = *)


end
